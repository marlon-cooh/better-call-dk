name: dataknow
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      - pandas
      - openpyxl
      - chromadb
      - openai
      - python-dotenv
      - numpy
      - tqdm

